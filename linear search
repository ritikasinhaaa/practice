#include<iostream>
using namespace std;
int main()
{
    int i, n,a[50],ele;
    cin>>n;
    for(i=0;i<n;i++)
    {
        cin>>a[i];
    }
    cin>>ele;
    for(i=0;i<n;i++)
    {
        if(a[i]==ele)
        {
            cout<<"ele present at "<<i+1<<"th position";
        }
        
    }
}
